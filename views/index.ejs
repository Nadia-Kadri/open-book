<!DOCTYPE html>
<html lang="en-us">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Open Book</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="styles/main.css">
  </head>

  <body>
    <div class="p-5 text-center bg-body-tertiary" id="header">
      <div class="container py-5">
        <h1 class="text-body-emphasis">Open Book</h1>
        <p class="col-lg-8 mx-auto lead">
          An ongoing collection of books I've read <br> and my thoughts on them!
        </p>
      </div>
    </div>

    <div class="container py-4">
      <div class="row justify-content-center">
        <div class="col-lg-9 col-sm-12 pb-2 border-bottom h6">
          Sort By:
            <a href="/?sort=title">title</a>, 
            <a href="/?sort=date">date</a>, 
            <a href="/?sort=rating">rating</a>
            <a href="/new" class="float-end">Add a new book</a>
        </div>
      </div>
    </div>

    <div class="container">
      <% books.forEach(book => { %>
        <div class="row justify-content-center pb-4">
          <div class="col-sm-2 pb-4 border-bottom">
            <img src="https://covers.openlibrary.org/b/isbn/<%= book.isbn %>-M.jpg" width="120px" class="img-fluid"/>
          </div>

          <div class="col-sm-3 pb-4 border-bottom">
            <h6 class="mb-0"><%= book.title %></h6>
            <p class="mb-0">by</p>
            <p><%= book.author %></p>
            <p><em>Date read: <%= format(book.date_read, "MMM d, yyyy") %></em></p>
            
            <p class="mb-0"><%= book.rating %>/5</p>
            <% for( let i = 0; i < Math.floor(book.rating); i++ ) { %>
              <i class="fa-solid fa-star" style="color: orange;"></i>
            <% } %>
            <% let remainder = 5 - book.rating %>
            <% if (remainder % 1 === 0) { %>
             <% for( let j = 0; j < remainder; j++ ) { %>
              <i class="fa-regular fa-star" style="color: orange;"></i>
             <% } %>
            <% } else { %>
              <i class="fa-solid fa-star-half-stroke" style="color: orange;"></i>
              <% for( let k = 0; k < Math.floor(remainder); k++ ) { %>
                <i class="fa-regular fa-star" style="color: orange;"></i>
               <% } %>
            <% } %>
            
          </div>

          <div class="col-lg-4 col-sm-7 pb-4 border-bottom">
            <p><%= book.review %></p>
            <a href="/delete/<%= book.id %>" class="btn btn-secondary btn-sm">Delete</a>
          </div>
        </div>
      <% }) %>
    </div>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
    <script src="https://kit.fontawesome.com/f7b39fcb95.js" crossorigin="anonymous"></script>
  </body>
</html>

